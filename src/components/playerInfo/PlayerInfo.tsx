import React from 'react'
import '../../css/gameSettings/playerInfo.css'

// have the name at the top

// the shape in the middle

const PlayerInfo = () => {
  return (
      <div className='playerInfo'>
          <section>
        <input/>
          </section>
    </div>
  )
}

export default PlayerInfo